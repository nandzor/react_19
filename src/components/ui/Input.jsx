// Mengimpor library React.
import React from 'react';

// Komponen Input yang menggunakan Tailwind CSS untuk styling
export function Input(props) {
  return <input className="form-control" {...props} />;
}
